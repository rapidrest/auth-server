---
apiVersion: v1
kind: Service
metadata:
  name: -services
  labels:
    app: 
    app.kubernetes.io/instance: -services
spec:
  ports:
    - port: 80
      targetPort: 3000
      name: nodejs
      protocol: TCP
  selector:
    app: 